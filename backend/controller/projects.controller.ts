const addPhoto = (req: any, res: any) => {
    try {
        const { image } = req.body
    } catch (error) {
        console.log("error => ", error)
    }
}

const getPhoto = (req: any, res: any) => {
    try {
        
    } catch (error) {
        console.log("error => ", error)
    }
}


const addComments = (req: any, res: any) => {
    try {
        
    } catch (error) {
        console.log("Error => ", error)
    }
}

const getComments = (req: any, res: any) => {
    try {
        
    } catch (error) {
        console.log("Error => ", error)
    }
}
